[package]
name = "fs-math"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Deterministic elementary functions with ULP budgets and FMA/subnormal/NaN policy."

[package.metadata.frankensim]
layer = "L0"

[dependencies]

[dev-dependencies]
fs-casebook = { path = "../fs-casebook" }
# Constellation reference oracle (test-only): independent Cephes-derived
# erf/erfc implementations for the BEDROCK special-functions battery.
fsci-special = { path = "../../../frankenscipy/crates/fsci-special", version = "=0.1.0" }

[lints]
workspace = true
