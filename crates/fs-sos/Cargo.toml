[package]
name = "fs-sos"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Proof-carrying optimization (fs-sos): sum-of-squares decomposition diagnostics, exact-global and radius-scoped certified value bounds, quadratic certificate construction, PSD feasibility, and linear Lyapunov verification."

[package.metadata.frankensim]
layer = "L4"

[dependencies]
fs-ivl = { path = "../fs-ivl" }
fs-math = { path = "../fs-math" }

[dev-dependencies]
fs-obs = { path = "../fs-obs" }

[lints]
workspace = true
