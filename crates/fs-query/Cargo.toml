[package]
name = "fs-query"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Geometry queries: closest point, certified raycasts, offsets, clearance with rigorous separation bounds, the thickness oracle, and curvature on every chart."

[package.metadata.frankensim]
layer = "L2"

[dependencies]
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-mesh = { path = "../fs-mesh" }
fs-obs = { path = "../fs-obs" }
fs-rep-frep = { path = "../fs-rep-frep" }
fs-rep-mesh = { path = "../fs-rep-mesh" }
fs-rep-sdf = { path = "../fs-rep-sdf" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }
fs-alloc = { path = "../fs-alloc" }

[lints]
workspace = true
