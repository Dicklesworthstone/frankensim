[package]
name = "fs-rep-voxel"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Occupancy/multi-material voxel charts on the sparse VDB substrate, exact Euclidean distance transforms, point clouds with normals, and FrankenNetworkx lattice/strut graphs with solid realization."

[package.metadata.frankensim]
layer = "L2"

[dependencies]
fnx-classes = { path = "../../../franken_networkx/crates/fnx-classes" }
fnx-runtime = { path = "../../../franken_networkx/crates/fnx-runtime" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-math = { path = "../fs-math" }
fs-rep-sdf = { path = "../fs-rep-sdf" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }
fs-alloc = { path = "../fs-alloc" }

[lints]
workspace = true
