[package]
name = "fs-modal"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Generalized vibration eigenproblem facility: K phi = lambda M phi with certified intervals and inertia-certified spectrum slicing."

[package.metadata.frankensim]
layer = "L1"

[dependencies]
fs-la = { path = "../fs-la" }
fs-math = { path = "../fs-math" }
fs-sparse = { path = "../fs-sparse" }

[dev-dependencies]
# Constellation reference oracle (test-only, dev-deps are layer-exempt):
# independent dense symmetric eigensolver for the casebook cross-check.
fsci-linalg = { path = "../../../frankenscipy/crates/fsci-linalg" }

[lints]
workspace = true
