[package]
name = "fs-govern"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Addendum governance as machine-readable data: the design principles (P1-P8), the governance rules, the nineteen proposals with kill metrics + owning beads, and the risk register (R1-R10) — each with a CI-gateable completeness audit so nothing survives unmeasured (Governance Rule 2 / design principle P8)."

[package.metadata.frankensim]
layer = "UTIL"

[dependencies]
fs-blake3 = { path = "../fs-blake3" }

[dev-dependencies]

[lints]
workspace = true
