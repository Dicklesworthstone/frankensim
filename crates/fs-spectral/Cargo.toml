[package]
name = "fs-spectral"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Versioned spectral problem admission, proposition-bound set-valued truth, and deterministic sheaf-Laplacian gap/conditioning health monitoring."

[package.metadata.frankensim]
layer = "L1"

[dependencies]
fs-blake3 = { path = "../fs-blake3" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-la = { path = "../fs-la" }
fs-qty = { path = "../fs-qty" }

[dev-dependencies]
fs-alloc = { path = "../fs-alloc" }
fs-math = { path = "../fs-math" }
fs-sparse = { path = "../fs-sparse" }

[lints]
workspace = true
