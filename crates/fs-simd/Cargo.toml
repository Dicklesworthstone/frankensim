[package]
name = "fs-simd"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "SIMD tiers: scalar baseline, NEON/AVX-512 capsules behind safe facades."

[package.metadata.frankensim]
layer = "L0"

[dependencies]
fs-substrate = { path = "../fs-substrate" }

[features]
# [F]-gated (bead wf9.3): SME2 streaming-mode GEMM prototype.
# Runtime-capability-gated on top of the flag; inert without hardware.
frontier-sme2 = []

[dev-dependencies]
fs-casebook = { path = "../fs-casebook" }

[[test]]
name = "sme2_battery"
required-features = ["frontier-sme2"]

[lints]
workspace = true
