[package]
name = "fs-schedule-e2e"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "CampaignSchedule: outward-bounded max-plus makespan analysis plus an explicitly Estimated value-of-information recommendation with distinct act, robust-stop, and deficient-menu outcomes."

[package.metadata.frankensim]
layer = "L6"

[dependencies]
fs-tropical = { path = "../fs-tropical" }
fs-voi = { path = "../fs-voi" }
fs-evidence = { path = "../fs-evidence" }

[dev-dependencies]

[lints]
workspace = true
