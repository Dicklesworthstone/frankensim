[package]
name = "fs-mesh"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Body-fitted tet meshing: BRIO incremental Delaunay on exact predicates, quality refinement, exact audits."

[package.metadata.frankensim]
layer = "L2"

[dependencies]
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-ivl = { path = "../fs-ivl" }
fs-math = { path = "../fs-math" }
fs-obs = { path = "../fs-obs" }
fs-rep-mesh = { path = "../fs-rep-mesh" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }
fs-alloc = { path = "../fs-alloc" }

[features]
# [F]-gated per the Ambition-Tag rule (bead wqd.18).
frontier-hexmesh = []

[lints]
workspace = true
