[package]
name = "fs-scene"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Static environment for robot experiments: keep-out and support bodies with exact interpenetration queries, robot-agnostic."

[package.metadata.frankensim]
layer = "L3"

[dependencies]
fs-ga = { path = "../fs-ga" }
fs-geom = { path = "../fs-geom" }
fs-query = { path = "../fs-query" }

[lints]
workspace = true
