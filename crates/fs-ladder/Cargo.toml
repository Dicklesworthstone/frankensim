[package]
name = "fs-ladder"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Context- and QoI-specific fidelity graphs with evidenced cost/discrepancy edges, deterministic explained queries, and lossless legacy-ladder embedding."

[package.metadata.frankensim]
layer = "L3"

[dependencies]
fs-blake3 = { path = "../fs-blake3" }

[dev-dependencies]

[lints]
workspace = true
