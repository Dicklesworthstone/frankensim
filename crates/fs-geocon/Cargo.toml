[package]
name = "fs-geocon"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Geometric constraint primitives: min-thickness, draft, symmetry-by-quotient, envelopes, certified volume — each with derivatives or certificates and its ASCENT constraint mapping."

[package.metadata.frankensim]
layer = "L4"

[dependencies]
fs-constraint = { path = "../fs-constraint" }
fs-evidence = { path = "../fs-evidence" }
fs-exec = { path = "../fs-exec" }
fs-geom = { path = "../fs-geom" }
fs-math = { path = "../fs-math" }
fs-obs = { path = "../fs-obs" }
fs-opt = { path = "../fs-opt" }
fs-query = { path = "../fs-query" }
fs-rep-frep = { path = "../fs-rep-frep" }

[dev-dependencies]
asupersync = { path = "../../../asupersync" }
fs-alloc = { path = "../fs-alloc" }

[lints]
workspace = true
